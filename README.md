# SETUP
If on NixOS, to make available gcc, gfortran, gdb etc:
`nix develop`

# BUILD
to build the app run:
`make`

# RUN
to run the app run:
`./app`

# About
This is an example of how to call a Fortran subroutine from C code,
using a bridge file.
